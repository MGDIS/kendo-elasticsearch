{
  "type": "object",
  "required": ["birthDate", "name", "siblings"],
  "properties": {
    "birthDate": {
      "type": "string",
      "format": "date-time",
      "chance": "birthday"
    },
    "name": {
      "type": "object",
      "required": ["firstName", "lastName"],
      "properties": {
        "firstName": {
          "type": "string",
          "faker": "name.firstName"
        },
        "lastName": {
          "type": "string",
          "faker": "name.lastName"
        }
      }
    },
    "city": {
      "type": "string",
      "faker": "address.city"
    },
    "emails": {
      "type": "array",
      "items": {
        "type": "string",
        "faker": "internet.email"
      }
    },
    "siblings": {
      "type": "integer",
      "minimum": 0,
      "maximum": 10
    }
  }
}
